# Documentation images

The images in this directory are generated from the synthetic
[`examples/field-notes`](../../examples/field-notes) fixture. They contain no
private Notion workspace data.

Keep related images at matching dimensions and display them side by side in
Markdown tables. Update screenshots only when the visible output changes, and
review both images before committing them.
